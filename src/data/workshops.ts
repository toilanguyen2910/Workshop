import type { Workshop } from '../types/workshop'

/** Thêm workshop vào đây hoặc nối API sau. Để `[]` sẽ hiện trạng thái không có kết quả như giao diện mẫu. */
export const workshops: Workshop[] = []
